name:          lambda
version:       0.1
cabal-version: >= 1.10

build-type: Simple

library
  build-depends:
    base       >= 4 && < 5,
    containers >= 0.6 && < 0.7

  exposed-modules:
    Lambda
    Subst

  hs-source-dirs: src

  default-language: Haskell2010

  ghc-options: -Wall
